#include "main.h"

/**
 * print_numbers - print 0 - 9 only using _putchar twice
 *
 * Return: Always 0 (success)
*/

void print_numbers(void)
{
	int x;

	for (X = 0; x <= 0; x++)
	{
	_putchar(x + '0');
	}
	_putchar('\n');
}
